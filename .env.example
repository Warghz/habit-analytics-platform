DATABASE_URL=postgresql://postgres:password@localhost/fastapi
# Optional. Defaults to DATABASE_URL and is converted to the asyncpg driver for Alembic.
ALEMBIC_DATABASE_URL=postgresql://postgres:password@localhost/fastapi
SECRET_KEY=replace-with-a-long-random-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
